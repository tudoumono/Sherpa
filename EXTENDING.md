# Sherpa をフォークして拡張する

Sherpa をフォーク／クローンして自組織向けに拡張する人が最初に読む1ページです。ライセンスは許可制の
source-available で改変可・上流への還流は任意（義務ではない・歓迎）。

## 読む順

1. **[CLAUDE.md](CLAUDE.md)** — 振る舞いの契約（本体・フォーク先とも守る基本ルール）。
2. **[docs/20-開発ハーネス.md](docs/20-開発ハーネス.md)** — 開発の型（提案→スライス→実装→敵対レビュー→ゲート→マージ）。
   フォークすると `CLAUDE.md`・本書・エージェント定義・`.claude/skills/rv/`（rv スキル）・
   `.github/workflows/ci.yml` も同梱されるので、同じ型でそのまま開発を続けられます。
3. **[docs/21-拡張の契約.md](docs/21-拡張の契約.md)** — どこを拡張してよいか（アナライザ・頭脳 provider・
   変換アーム・MCP ツールの4面）・命名規約・版署名・保証範囲。

## すぐ試す

- サンプル拡張: `sherpa/ingest/analyzers/sample_ext_dummy.py`（`<prefix>_*.py` 規約に沿った最小のアナライザ実例。
  常駐・既定で有効）。
- 契約検査: `make verify-extension`（拡張面ごとに「確認した契約」または「未確認」を表示。違反は非ゼロ終了）。

## レビュアーの選び方

敵対レビュー（マージ前に必ず1巡以上）は、環境にある実装を使います。外部エージェント CLI（Codex 等）が使える
環境ではそれを使い、使えない環境では Claude 実装のレビュアー（`.claude/agents/adversarial-reviewer.md`）を選びます。
どちらも役割の入出力は同じ（`docs/20-開発ハーネス.md` §2・§3）——レビュアーが変わっても運用の型は変わりません。

## 核と拡張面の違い

- **核**（鏡モデル・グラフ語彙・画面の原則・認証と隔離）は自己責任で改変してよい領域です。上流はこの互換性を
  保証しません。
- **拡張面**（アナライザ・頭脳 provider・変換アーム・MCP ツール）は上流が契約テストで守る領域です。まずこちらに
  収まらないか検討してください（詳細は [docs/21-拡張の契約.md](docs/21-拡張の契約.md)）。

## 上流の取り込み

公開リポにリリース区切りでタグ（`vX.Y.Z`）＋ CHANGELOG を付けます。タグ単位で取り込んでください。
