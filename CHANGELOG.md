# Changelog

このリポジトリの変更点を記録する。書式は [Keep a Changelog](https://keepachangelog.com/) に準拠する。

## [Unreleased]

### 追加

- アナライザを大幅拡張: COBOL の固定形式論理行正規化、SQL/DDL・EXEC SQL、設定ファイル
  （properties/YAML/XML）、C・C#、Java フレームワーク表（Spring/MyBatis/Struts/TERASOLUNA）、
  画面テンプレート（JSP/HTML）・JS・CSS、シェル/バッチ（Spring Batch 含む）、VB.NET/VB6/VBA を
  追加し、影響分析・検索の対象言語を広げた。
- チャット履歴の検索（会話一覧の検索欄）を追加した。
- Codex 実行が「途中経過」だけで閉じたターンを自動で続ける機能と、`--output-schema` による
  完了判定の構造化を追加した。
- 調査結果の集約: API 経路の清書・査読への情報欠落を是正し、調査状態を1つにまとめ、独立した
  ツール呼び出しを並列実行できるようにした。
- チャットの同時実行上限を管理者画面から編集できるようにした。
- Codex 実行を利用者間で直列化していたのを撤去し、実行ごとの作業領域で並走できるようにした。
- 開発ハーネス（提案→スライス→実装→敵対レビュー→ゲート→マージという開発の型）を正典化し、
  敵対レビューを型どおりに起動する `rv` スキル、段階別のテストゲート（`make gate-slice` /
  `make gate-merge` / `make gate-release`）、push・PR ごとの CI、フォーク先が拡張してよい面
  （アナライザ・頭脳 provider・変換アーム・MCP ツール）の契約と `make verify-extension` を
  追加した。

### 変更

- 上部ナビの収まらないタブを「その他」へ畳むなど、UI の可読性・操作性を改善した。
- 秘匿ファイル（鍵・資格情報等）の除外判定を1箇所に集約し、取り込み・検索・派生生成の
  各経路で一貫して適用されるようにした。

## 過去の変更

このファイルの整備前の変更は `git log` を参照。
