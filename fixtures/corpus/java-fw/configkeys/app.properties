# S3'（キー単位 Config children）の最小サンプル（基底設定・env/dev・env/prod が上書きする）。
db.url=jdbc:postgresql://localhost/db
tax.rate=0.08
# 重複キー（後続は無視・最初の行が採用される）
tax.rate=0.99
# 継続行（行末の `\` で次行と結合）
long.note=first part \
    second part
